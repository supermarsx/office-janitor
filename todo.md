# TODO

- CI workflow split is deferred for now per current priorities; when ready, add the spec-aligned `format.yml`, `lint.yml`, `test.yml`, `build.yml`, `publish-pypi.yml`, and `release.yml` workflows to replace the monolithic `.github/workflows/ci.yml`.
